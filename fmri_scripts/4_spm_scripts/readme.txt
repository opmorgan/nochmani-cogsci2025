Run 1st level on all right handers, and left handers, without gray matter masking:

Preprocessing was run with fmriprep, with fieldmap correction.

8mm smoothing (SPM default, following B & L)

No gray matter mask. (Whole brain mask used)

Contrast columns:
approach-pos, approach-neg, avoid-pos, avoid-neg, baseline, approach-pos...

No nuisance regressors.